import { Smooch, Forum } from "next/font/google";

export const smooch = Smooch({
  subsets: ["latin"],
  display: "swap",
});

export const forum = Forum({
  subsets: ["latin"],
  display: "swap",
});
